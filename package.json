{
  "name": "playpals",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "PlayPals — a hub of 1P-vs-CPU / 2P mini-games (Phaser 3 + Vite + Capacitor + AdMob).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview --port 4173",
    "cap:add:android": "cap add android",
    "cap:sync": "npm run build && cap sync"
  },
  "dependencies": {
    "@capacitor-community/admob": "^8.0.0",
    "@capacitor/core": "^8.4.1",
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "@capacitor/android": "^8.4.1",
    "@capacitor/cli": "^8.4.1",
    "typescript": "^6.0.0",
    "vite": "^8.0.0"
  }
}
